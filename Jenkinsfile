pipeline {
  agent any
  stages {
    stage('copia de ficheros') {
      steps {
        sh 'echo "adios"'
        sh 'cp -r /var/jenkins_home/workspace/CRUD2/CRUD_mysqli /var/www'
      }
    }

  }
}
